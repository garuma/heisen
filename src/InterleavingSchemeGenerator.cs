
using System;

namespace Heisen
{
	public static class InterleavingSchemeGenerator
	{
		/* Return a suite of list of stepping instruction that are in the 
		 * form (thread to instrument, #instruction to execute)
		 */
		public static IEnumerable<IEnumerable<Tuple<int, int>>> GenerateInterleavings ()
		{
			
		}
	}
}